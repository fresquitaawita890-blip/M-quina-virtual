name: PC_GAMER_FINAL_ROBLOX
on: workflow_dispatch
jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: 1_Configurar_PC
        run: |
          # Configuramos el usuario runneradmin con clave para que te deje entrar
          net user runneradmin RobloxVM2026
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          
      - name: 2_Instalar_Graficos_Mesa3D
        run: |
          # Esto es lo que hace que Roblox NO se cierre. Instala el motor gráfico por software.
          choco install mesa3d-softpipe -y
          
      - name: 3_Abrir_Tunel_Directo
        run: |
          # Usamos un servidor alternativo para que no te de el error "connection refused"
          choco install ngrok -y
          ngrok config add-authtoken 39eVkh665hfvR3NNdqK6yFbG3B7_5NqkDibvo8CtNxNEkmxwR
          Start-Process ngrok -ArgumentList "tcp 3389"
          Start-Sleep -Seconds 45
          
      - name: 4_OBTENER_DATOS_YA
        run: |
          $address = (Invoke-RestMethod http://localhost:4040/api/tunnels).tunnels[0].public_url
          $clean = $address -replace "tcp://", ""
          echo "--------------------------------------------------"
          echo "✅ PC LISTA PARA ROBLOX"
          echo "🌐 DIRECCION: $clean"
          echo "👤 USUARIO: runneradmin"
          echo "🔑 CLAVE: RobloxVM2026"
          echo "--------------------------------------------------"
          
      - name: 5_No_Cerrar
        run: Start-Sleep -Seconds 21600
        
