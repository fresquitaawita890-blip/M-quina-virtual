name: GitHub_PC_Gamer_Pro
on: workflow_dispatch
jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: 1_Configurar_Sistema
        run: |
          net user Administrator RobloxVM2026
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          
      - name: 2_Instalar_Graficos_Mesa3D
        run: |
          # Esto instala el motor para que Roblox corra sin tarjeta de video real
          choco install mesa3d-softpipe -y
          
      - name: 3_Abrir_Tunel_Ngrok
        run: |
          choco install ngrok -y
          ngrok config add-authtoken 39eVkh665hfvR3NNdqK6yFbG3B7_5NqkDibvo8CtNxNEkmxwR
          Start-Process ngrok -ArgumentList "tcp 3389"
          Write-Host "Esperando 30 segundos a que el tunel se estabilice..."
          Start-Sleep -Seconds 30
          
      - name: 4_OBTENER_DATOS_CONEXION
        run: |
          $address = (Invoke-RestMethod http://localhost:4040/api/tunnels).tunnels[0].public_url
          $cleanAddress = $address -replace "tcp://", ""
          echo "--------------------------------------------------"
          echo "✅ PC LISTA PARA JUGAR"
          echo "🌐 DIRECCION: $cleanAddress"
          echo "👤 USUARIO: Administrator"
          echo "🔑 CLAVE: RobloxVM2026"
          echo "--------------------------------------------------"
          
      - name: 5_Mantener_Viva
        run: Start-Sleep -Seconds 21600
        
